module.exports = {
  getAll: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },

  getData: (res,response, name)=>{
    const form = {
        name,
        response,
    };
    res.json(form);
  },
  postProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  addProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  deleteProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  putProduct: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getTransaction: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  postTransaction: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getsubTrans: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  postsubTrans: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getsubTransByID: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getsubTransByDate: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  updateUser: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  updateDriver: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getDriverByID: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getDriverItemPhoto: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  
  getOwnerByID: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getsubTransByIDUser: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  updatesubTransAccept: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  selecedbyDate: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  getUserByID: (res, response, status) => {
    const form = {
      status,
      response
    };
    res.json(form);
  },
  
  resetPassword:(res,response,status)=>{
    const form={
      status,
      response
    }
    res.json(form);
  },
  updateUserPassword:(res,response,status)=>{
    const form={
      status,
      response
    }
    res.json(form);
  },
  updateOwnerPassword:(res,response,status)=>{
    const form={
      status,
      response
    }
    res.json(form);
  },
 
  getUserbyEmail:(res,response,status)=>{
    const form={
      status,
      response
    }
    res.json(form);
  }
  
};






